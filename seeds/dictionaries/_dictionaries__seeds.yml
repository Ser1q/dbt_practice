seeds:
  - name: city_region
    description: Cities and regions
    config:
      delimiter: ";"
      column_types:
        city: varchar(50)
        region: varchar(50)
        updated_at: timestamp
    columns:
      - name: city
        tests:
          - not_null
      - name: region
      - name: updated_at
  - name: avia_passengers
    description: Passenger that work in avia company
    config:
      column_types:
        passenger_id: varchar(11)